#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (n); i++)
using namespace std;
using ll = long long;
int INF = 1001001001;

int main()
{
    int n, d, x;
    cin >> n >> d >> x;
    int a[n];
    rep(i, n)
    {
        cin >> a[i];
    }

    return 0;
}