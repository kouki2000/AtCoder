#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (n); i++)
using namespace std;
using ll = long long;
using P = pair<int, int>;
int INF = 1001001001;

int main()
{
    int n, m;
    cin >> n >> m;
    vector<int> k(n);
    int a[100][100];
    int search[100];
    rep(i, n)
    {
        cin >> k[i];
        int cnt = k[i];
        rep(j, cnt)
        {
            cin >> a[i][j];
            rep(l, cnt)
            {
            }
        }
    }

    return 0;
}